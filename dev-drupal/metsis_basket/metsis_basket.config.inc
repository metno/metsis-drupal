<?php

global $metsis_conf;

/*
 * These must be configurable from the admin interface
 */

/**
 * metsis basket server on ARES
 */
define('METSIS_BASKET_SERVER', $metsis_conf['metsis_basket_server']);
define('METSIS_BASKET_SERVER_PORT', $metsis_conf['metsis_basket_server_port']);
define('METSIS_BASKET_SERVER_SERVICE', $metsis_conf['metsis_basket_server_service']);
