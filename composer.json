{
    "name": "metsis/metsis-drupal",
    "type": "drupal-metsis-module",
    "description": "METSIS drupal modules",
    "license": "GPL-2.0-or-later",
    "scripts": {
        "sniff": "phpcs --colors -n --standard=Drupal --extensions=php,module metsis",
        "snifferfix": "phpcbf --colors -n --standard=Drupal --extensions=php,module metsis"
    }
}
